{
  "name": "technocode-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Coding practice and AI mock interview platform.",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend && npm install --prefix worker",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:worker": "npm run dev --prefix worker",
    "build": "npm run build --prefix frontend",
    "test": "npm test --prefix backend && npm test --prefix worker && npm run lint --prefix frontend"
  }
}
